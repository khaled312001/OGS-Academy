cd /home/u352429374/domains/ogs-academy.com/public_html && php artisan tinker --execute="try { \Mail::raw('🧪 SMTP Test from OGS Academy production — connection is working. ' . now()->toDateTimeString(), function(\$m){ \$m->to('info@ogs-academy.com')->subject('✅ Test: SMTP Configuration Working'); }); echo 'SUCCESS: Test email sent to info@ogs-academy.com'; } catch (\Throwable \$e) { echo 'FAIL: ' . \$e->getMessage(); }"
